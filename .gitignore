__pycache__/
*.pyc
*.pyo
*.egg-info/
.eggs/
dist/
build/
*.so

# Large data files — tracked separately (HuggingFace)
data/route_balance/latency_data/
data/route_balance/training_data/
data/route_balance/training_data_with_ref/
data/route_balance/training_logs/
data/route_balance/*.json
data/route_balance/lmsys_*.json
data/route_balance/lmsys_*.jsonl
data/route_balance/broadcast_*.json

# Keep test dataset
!data/route_balance/best-route-v3-test-500.jsonl

# Models (downloaded at deploy time)
models/

# Experiment output
experiment_output/

# Logs
*.log

# IDE
.vscode/
.idea/

# Paper build artifacts
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.fdb_latexmk
paper/*.fls
paper/*.synctex.gz
paper/*.pdf

# Paper and docs (local only, not in repo)
paper/

# Claude session memory (local only)
.claude/
CLAUDE.md
AGENT.md
AGENTS.md

# Local-only working directories (not for public release)
*_paper/
*_dev_snapshot_*/

# Editor / OS
*.swp
.DS_Store

# --- Privacy / host-specific (provide your own; provision via the CloudLab profile) ---
# Address manifests (SSH hosts, per-host model placement) — fill from the .template files
route_balance/config/host_configs.json
route_balance/config/**/host_configs*.json
route_balance/config/**/model_deployment*.json
**/*.manifest.xml
**/*.xml
# CSD3 / SLURM scripts are institution-specific; offline training uses the python trainers
route_balance/exp/route_balance/csd3_*.sh
# Stale archived configs (contained hardcoded cluster paths)
route_balance/config/route_balance/archive/
*.bak
*.bak.*
route_balance/exp/route_balance/archive/
scripts/transform_block_to_route_balance.sh
PAPER_REPRODUCTION_MAP.md
!route_balance/config/host_configs.template.json
!route_balance/config/route_balance/model_deployment.template.json
